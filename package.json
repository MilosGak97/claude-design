{
  "name": "movezilla-blog-designs",
  "private": true,
  "version": "1.0.0",
  "description": "Static blog design explorations for MoveZilla, served as a simple Railway web service.",
  "scripts": {
    "start": "serve -s . -l $PORT",
    "start:local": "serve -s . -l 3000"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
